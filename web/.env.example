# 前端构建期环境变量（Vite 读取 web/.env 或 shell 注入；本文件为模板，复制为 web/.env 后按需填）
#
# 原型 iframe origin（PROTO_ORIGIN）。解析优先级：
#   显式 VITE_PROTO_ORIGIN  >  开发默认 http://localhost:8081  >  生产默认 window.location.origin
#
# · 开发（make dev / Vite :8080 + Flask :8081）：留空即可，自动用 http://localhost:8081。
# · 生产 Nginx「同域反代 /proto」（推荐，与宿主同源）：留空即可，自动用当前站点 origin。
# · 生产后端在独立 host:port 提供（不反代）：填 http://<host>:<PORT>（PORT 即后端 PORT）。
#VITE_PROTO_ORIGIN=
